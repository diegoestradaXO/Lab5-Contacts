<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.diego.appcontactos">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Models.AddContactActivity">
        </activity>
        <activity android:name=".Models.ContactInformation">
        </activity>
        <activity android:name=".Models.MailActivity">
        </activity>

        <provider
                android:name=".ContactProvider"
                android:authorities="com.example.diego.appcontactos.ContactProvider"/>

        <activity android:name=".UpdateContactActivity">
        </activity>
    </application>

</manifest>